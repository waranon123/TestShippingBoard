{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "vercel-build": "vite build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}